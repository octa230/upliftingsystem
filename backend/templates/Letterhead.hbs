<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Letter Head</title>
  <style>
    :root {
      --primary-color: #FFC200;
      --secondary-color: #6c757d;
      --border-color: #e0e0e0;
      --accent-color: #ffcb00;
      --text-color: #333;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      font-size: 12px;
      line-height: 1.5;
    }

    body {
      background-color: #ffffff;
      padding: 20px;
      min-height: 297mm; /* A4 height */
      width: 210mm; /* A4 width */
      margin: 0 auto;
      color: var(--text-color);
    }

    /* Header Styles */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: flex-center; 
      border-bottom: 2px solid var(--border-color);
      padding-bottom: 15px;
      margin-bottom: 30px;
    }

    .company-info {
      display: flex;
      flex-direction: column; /* Stack logo and company info vertically */
      justify-content: center;  /* Align items at the top */
      margin-left: 15px;
    }

    /* .company-details {
      font-size: 11px;
      color: var(--secondary-color);
      line-height: 1.5;
      margin-top: 10px;
    } */

    .logo-container {
      flex: 0 0 200px;
      width: 40%;
      text-align: center;
      align-items: center;
    }

    .logo {
      max-width: 100%;
      object-fit: cover;
      align-self: center;
      height: auto;
      max-height: 120px;
    }

    /* Content Section */
    .content-area {
      min-height: 180mm; /* Space for content */
      padding: 20px 0;
      margin-bottom: 50px; /* Space for footer */
    }

    /* Footer Styles */
    .footer {
      background-color: var(--primary-color);
      color: white;
      text-align: center;
      padding: 3px;
      margin-top: auto;
      border-radius: 5px;
    }

    .footer-text p {
      margin: 5px 0;
      font-size: 8px;
      color: #fff;
    }

    .footer-text a {
      color: var(--accent-color);
      text-decoration: none;
    }

    /* Responsive Styling for Print */
    @media print {
      body {
        margin: 0;
        padding: 15px;
      }

      .header {
        padding-bottom: 5px;
        margin-bottom: 20px;
        align-items: center;
      }

      .footer {
        position: fixed;
        bottom: 0;
        left: 0;
        right: 0;
        margin: 0;
        border-radius: 0;
        font-size: 9px;
      }

      .company-details {
        font-size: 10px;
        line-height: 1.3;
      }
    }

    /* Styling for Address Section */
    .address {
      margin-top: 5px;
      font-weight: bold;
      color: var(--primary-color);
    }

    .contact-info p {
      font-size: 12px;
      line-height: 1.5;
      color: var(--secondary-color);
    }
    .watermark {
        position: fixed;
        bottom: 30%;
        left: 0;
        right: 0;
        text-align: center;
        color: #5F904D;
        font-size: 60px;
        font-weight: bold;
        opacity: 0.1;
        transform: rotate(-30deg);
        z-index: -1;
      }
  </style>
</head>
<body>

<header class="header">
  <!-- Logo Section -->
  <div class="logo-container">
    <img class="logo" src={{logo}} alt="Company Logo"/>
  </div>

  <!-- Company Info Section -->
  <div class="company-info">
    <div class="company-details">
      <p class="address">{{name}}</p>
      <p>{{address}}</p>
      <p>Email: {{email}}</p>
      <p>Phone: {{phone}} | {{mobile}}</p>
      <p>{{#if website}}Website: <a href="{{website}}" target="_blank">{{website}}</a>{{/if}}</p>
    </div>
  </div>
</header>

<div class="content-area">
  <div class="watermark">UPLIFTING FLORAL STUIDIO</div>
  <!-- This is where letter content would go -->
</div>

<footer class="footer">
  <div class="footer-text">
    <p>Thank you for choosing our services!</p>
    <p>For any inquiries, please contact us on the above addresses.</p>
  </div>
</footer>

</body>
</html>
